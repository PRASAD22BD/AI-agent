FROM n8nio/n8n:latest

# Use official n8n defaults but ensure port 5678 is exposed for Render
ENV N8N_PORT=5678
ENV N8N_RUNNERS_ENABLED=true

EXPOSE 5678

# No custom CMD needed, official entrypoint is best
